#!/bin/bash

if [ $# -ne 4 ]; then
    echo "$ URPortal.dockerRun <path-to-new-mysql-directory> <path-to-repository> <mysql-port> <webpage-port>"
    exit 1
fi

docker run -u ${UID}:0 -p ${3}:3306 -p ${4}:8080 -e MYSQL_ROOT_PASSWORD='toor' -v ${1}/web-portal/webpage:/var/www/html  -v ${2}:/var/lib/mysql/ gabrielcammany/ur-portal
